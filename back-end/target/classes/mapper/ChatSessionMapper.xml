<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jaxon.back_end.chat.mapper.ChatSessionMapper">

    <select id="findByUserIdAndUserType" resultType="com.jaxon.back_end.chat.dto.ChatSessionDTO">
        SELECT
            id,
            title,
            create_time AS createTime
        FROM chat_session
        WHERE user_id = #{userId}
          AND user_type = #{userType}
          AND is_deleted = 0
        ORDER BY update_time DESC, id DESC
    </select>

    <insert id="insertNewSession" useGeneratedKeys="true" keyProperty="id" keyColumn="id">
        INSERT INTO chat_session (user_id, user_type, title)
        VALUES (#{chatSession.userId}, #{chatSession.userType}, #{chatSession.title})
    </insert>

    <update id="deleteSession">
        UPDATE chat_session
        SET is_deleted = 1
        WHERE id = #{sessionId} 
    </update>

    <update id="resetTitle">
        UPDATE chat_session
        SET title = #{newTitle}
        WHERE id = #{sessionId}
    </update>

</mapper>
