<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jaxon.back_end.identity.mapper.EmployeeMapper">

    <select id="findByUsername" resultType="com.jaxon.back_end.identity.entity.Employee">
        SELECT id, username, password, department, create_time, update_time, is_deleted
        FROM employee
        WHERE username = #{username}
          AND is_deleted = 0
        LIMIT 1
    </select>

</mapper>
